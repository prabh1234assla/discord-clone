export type AvatarInit = {
    src: string
    alt: string
    fallback: string
}

export type AspectRatioInit = {
    ratio: number
}

export type HoverCardInit = {
    trigger: string
    content: string
}